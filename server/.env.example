# Server Configuration
PORT=30001

# Scheduler Configuration
# RSS_REFRESH_INTERVAL=600000  # Refresh interval in ms (default: 10 minutes)
# DISABLE_SCHEDULER=true       # Set to 'true' to disable background fetching
